// como fazemos para mostrar no console todos esses nomes individualmente?
const nomes = ["Angelina", "Pierre", "Maria"];

// vamos utilizar um loop for()
for(let i = 0; i < nomes.length; i++){
    console.log(` ${i}: Nome é ${nomes[i]}`);
}

